# Vercel Production Environment Variables
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Database Configuration - SUPABASE PRODUCTION
# Using pgBouncer pooler (port 6543) for connection pooling
# pgbouncer=true enables transaction pooling mode
# connection_limit=5 limits max connections per Prisma client instance
# pool_timeout=10 sets connection timeout to 10 seconds
# connect_timeout=10 sets initial connection timeout
DATABASE_URL="postgres://postgres.uyrmvvwwchzmqtstgwbi:QtMVSsu8uw60WRjK@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=5&pool_timeout=10&connect_timeout=10"

# NextAuth Configuration
NEXTAUTH_URL="https://tarl.openplp.com"
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"

# Server Configuration
NODE_ENV="production"

# File Upload Configuration
UPLOAD_DIR="/tmp/uploads"
MAX_FILE_SIZE="5242880"

# Session Configuration
SESSION_TIMEOUT="86400000"

# Suppress Warnings
SUPPRESS_ANTD_WARNING="true"

# Build Configuration
NODE_OPTIONS="--max-old-space-size=1024"
